# 112.Tuples Intro - Python tutorial 109
# tuples are used when we dont need to change the values once stored in the data structure
# for example if we have tos tore the days of week then we will use tuple instead of arrays or list
# tupes are faster then lists
example = ('monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday')
# methods that tuples can have
# count,index,len,slicing
# methods that tuples can not have
# append, insert, pop, remove
# tuples are immutable